import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
    main: {
        
        display: 'flex',
        background: '#e0f7fa',
        minHeight: '150vh'
    },
    header: {
        textAlign: 'center',
    },
    
}));

export default styles;