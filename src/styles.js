import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles(() => ({
    main: {
        
        display: 'flex',
        background: 'grey',
        minHeight: '150vh'
    },
    header: {
        textAlign: 'center',
    },
    
}));

export default styles;